<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <header class="page-header">
    <nav>

      <div class="site-nav">
        <ul class="dropdown menu" data-dropdown-menu>
          <li><%= link_to "Home", root_path, class: "button" %></li>
          <li>
            <a href="#" class="button" data-dropdown="drop">Browse</a>
            <ul id="drop" class="menu">
              <li><%= link_to "All Products", products_path, class: "button" %></li>
              <li><%= link_to "All Categories", categories_path, class: "button" %></li>
              <li><%= link_to "All Merchants", users_path, class: "button" %></li>
            </ul>
          </ul>
          <div class=login>
            <% if session[:user_id] %>
            <%= link_to "Log out", logout_path, method: "delete"   %>
            <% else %>
            <%= link_to "Login with Github", "/auth/github", class: "button"%>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <%= yield %>

  </body>
  </html>
