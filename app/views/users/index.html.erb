<section>
  <h1>All Merchants:</h1>
  <p>
    <ul class="merchants">
      <% @users.each do |user| %>
      <li>
        <%= @products %>
        <%= link_to(user.username, user_path(user.id), alt: 'link to') %>
        <% end %>
      </li>
    </ul>
  </p>

</section>
