class User < ApplicationRecord
#has_many :products
# validate username presence and uniqueness
# validate email presence and uniqueness

# self method to get the info from github auth_hash
# user = new
# user.provider = provider
# user.uid = auth_hash['uid']
# user.username = auth_hash['info']['username']
# user.email = auth_hash['info']['email']

end
